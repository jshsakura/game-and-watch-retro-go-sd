# 작업 지시서 — 32X, "더 당길 구석" 조사 (CPU 전용, 기기·도커 금지)

작성: 32X 세션. 대상: GLM 세션. 워크트리: `/tmp/claude-1001/-home-ubuntu-app-jupyterLab-notebooks-game-and-watch-retro-go-sd/5c3bf51f-1dbd-4cf4-bec3-d964120a0efa/scratchpad/wt32x` (브랜치 `exp/32x-d32xr`)

## 지켜야 할 경계 (어기면 다른 세션 작업이 깨집니다)

- **기기(rpi-genie5) 금지.** openocd/gnwmanager 호출 금지. 32X 세션이 A/B 측정 중입니다.
- **도커 빌드 금지.** `arm32x.sh build`, `make release DOCKER=1` 금지. 컨테이너와 `build/`가 공유됩니다.
- **커밋·푸시 금지.** 조사만. 파일을 고쳐야 하면 워크트리 사본을 만들어 거기서.
- QEMU 리그(`tools/m7_qemu_rig/run_32x.sh`)는 CPU만 씁니다 — **이건 마음껏 쓰세요.**

## 이 트리의 규칙 (오늘 하루에 여섯 번 데었습니다)

1. **잰 것이 만든 것인지 먼저 증명하세요.** 오늘 나온 실패 여섯 개가 전부 이것이었습니다 — 리그가 소스 파일 하나를 안 컴파일, 프로파일러가 기기에 없는 클론에 14% 청구, ELF와 플래시 이미지 불일치, 코어 파일과 펌웨어 불일치, 가드를 무시하는 코어를 가드 카운터로 측정. **A/B 두 arm은 반드시 `cmp`로 다른 바이너리임을 확인**하고, 노브가 안 닿았을 가능성을 먼저 의심하세요.
2. **추측을 결론으로 쓰지 마세요.** 확인 못 한 건 "확인 못 함"이라고 쓰세요. 오늘 "형제 코어니까 똑같겠지" 하나가 확정 수치 전체를 철회 직전까지 몰고 갔습니다.
3. **차이가 정확히 0이면 그 코드가 안 도는 겁니다.** 정상이 아니라 배선 실패를 의심하세요.
4. 결과는 `파일:줄` + 재현 명령으로. 숫자에는 출처(로그 파일 경로)를 다세요.

## 과제 1 (우선) — D32XR이 200프레임 뒤 멈추는 이유

**배경.** D32XR(둠 리메이크 엔진, 양쪽 SH-2로 렌더 분산)이 우리 코어에서 안 뜹니다. 오늘 확정된 것:

- picodrive의 BIOS 흉내 스텁으로는 SH-2가 **402 insn/frame**(대기 루프)
- **진짜 32X BIOS 3종**(`scratchpad/bios32x/.../32X BIOS/`, 68K 256B + 마스터 2KB + 슬레이브 1KB)을 물리면 **167,287 insn/frame** — 게임이 실제로 돕니다
- 그런데 **~200프레임(≈3.5초) 뒤 `sh2=0`**으로 떨어지고, 프레임버퍼는 끝까지 전부 0
- 공식 v3.1 롬(무수정, 5 MiB, SSF 매퍼 리그에 임시 배선) 으로도 **동일 재현** → 자체 빌드/wad 수술은 원인이 아님

**롬 두 개** (`scratchpad/roms32x/`): `d32xr.32x`(공식 v3.1 패치 결과, 5 MiB) · `d32xr_bench.32x`(맵1만 남긴 자체 빌드, 4 MiB)

**재현 명령**:
```sh
cd <워크트리>
S=/tmp/claude-1001/-home-ubuntu-app-jupyterLab-notebooks-game-and-watch-retro-go-sd/5c3bf51f-1dbd-4cf4-bec3-d964120a0efa/scratchpad
RIG_32X_BIOS="$S/bios32x/Sega - Mega Drive - CD - 32X - BIOS Files/32X BIOS" \
RIG_TIMEOUT=3600 RIG_OUT=tools/m7_qemu_rig/build/glm_a \
  bash tools/m7_qemu_rig/run_32x.sh "$S/roms32x/d32xr_bench.32x" 300 > /tmp/glm_a.log 2>&1
```
5 MiB 롬을 쓸 땐 `RIG_32X_SSF=1`도 함께.

**알아낼 것** (순서대로):
1. **정확히 몇 번째 프레임에서 멈추는가**, 그리고 그 직전 몇 프레임에서 SH-2가 뭘 하고 있었는가. `SH2_PC_HIST=1`은 전체 구간 합계라 전이가 안 보입니다 — **구간을 나눠 두 번 돌리세요**(예: 190프레임까지 / 300프레임까지 각각 히스토그램을 뜨고 차분).
2. **SH-2가 왜 0이 되는가.** picodrive에서 SH-2가 실행을 멈추는 경로는 몇 개뿐입니다 — RES 비트, SH2 standby/sleep(`sh2_regs[0] & 0x20` 계열), 어댑터 비활성(ADEN), 또는 스케줄러가 안 돌리는 경우. `pico/32x/32x.c`의 `p32x_run_cpus`/`Pico32xStateLoad` 부근을 읽고 **어느 조건에 걸리는지 코드로 지목**한 뒤, 리그에 `printf` 하나 넣어 확인하세요.
3. **화면이 왜 한 번도 안 뜨는가.** `Pico32xDrawMode`가 무엇으로 설정되는지, `PicoDrawSetOutBuf`가 32X 경로로 재적용되는지(`emu_32x_startup()` 주석 참고 — 이걸 안 하면 picodrive 내부 버퍼에 그리고 화면은 검게 남습니다). 리그가 그 재적용을 하는지 확인하세요. **이게 원인이면 SH-2 정지와는 별개의 두 번째 버그입니다.**
4. **비교군을 쓰세요.** 같은 리그에서 정품 둠(`roms32x/doom.32x`)은 **GATE3 PASS, sh2≈69,878/frame**입니다. 두 롬의 로그를 나란히 놓고 갈라지는 첫 지점을 찾으세요.

**산출물**: 원인 후보 1~2개를 `파일:줄`로 지목 + 각각을 확정/반증할 실험 1개씩. 고치지는 마세요.

## 과제 2 — msh2 인터프리터에 실제로 남은 구석이 있는가

**배경.** 7월 판정: 프레임 24.4M 사이클 중 msh2가 67.9%, 명령당 ~92 사이클, 메모리는 그중 1/4, 인터프리터는 이미 ITCM. 손튜닝 바닥값이 명령당 45라 해도 ×1.5가 천장. **이 판정을 뒤집으라는 게 아니라, 숫자를 오늘 도구로 다시 세우라는 것입니다.**

**할 일**: 정품 둠을 리그에서 돌려(`roms32x/doom.32x`, 400~600프레임) **SH-2 게스트 명령 분포**를 뽑으세요(`SH2_PC_HIST=1`은 PC 기준이니, 필요하면 디스어셈블과 대조해 **오퍼코드 종류별 빈도**로 집계). 답할 질문:

- 상위 10개 오퍼코드가 전체의 몇 %인가?
- 그중 **분기·메모리 접근·나머지**의 비율은?
- picodrive의 디스패치(`cpu/sh2/mame/sh2pico.c`)에서 그 상위 오퍼코드들이 **함수 호출인가 인라인인가**? 호출이면 호출당 오버헤드가 몇 명령인가?
- SNES에서 이긴 방식(오퍼코드당 C 래퍼를 엔진에 흡수)이 여기서 **적용 가능한 모양인가**?

**주의**: 리그는 명령 수만 셉니다. 캐시·대기는 못 봅니다(`docs/OPTIMIZATION_LEDGER.md` 측정 규율 첫 줄). 그러니 결론은 **"명령 수 기준 상한"**으로만 쓰고, 기기 사이클로 환산하지 마세요.

**산출물**: 오퍼코드 분포표 + "여기에 N% 여지가 있다/없다"를 근거와 함께. 없으면 없다고 쓰는 게 정답입니다 — 오늘 이 트리에서 가장 값어치 있었던 결과 절반이 "없다"였습니다.

## 과제 3 (여유 있으면) — 우리 picodrive가 업스트림보다 뒤처졌는가

`external/picodrive`는 우리 포크입니다. 업스트림(notaz/picodrive)과 **32X 관련 파일만** 비교해서, 우리가 못 받은 수정 중 **부팅·호환성·속도**에 관계된 게 있는지 목록화하세요. 코드는 건드리지 말고 목록만.

---

## 보고 형식

각 과제마다: **결론 한 줄 → 근거(파일:줄, 로그 경로, 수치) → 확인 못 한 것**. 추측으로 빈칸 채우지 마세요. 막히면 막힌 지점을 쓰세요 — 그게 다음 사람에게 제일 쓸모 있습니다.
