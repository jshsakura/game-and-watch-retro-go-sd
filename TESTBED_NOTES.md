# Moved

The full "how this build differs from upstream" overview now lives in the README:
**[README — How this fork differs from upstream](README.md#how-this-fork-differs-from-upstream)**.

Release notes list only the changes in each build; this file used to be the
release-notes body and is kept as a pointer so old links keep working.

> Install: flash `retro-go_update.bin`. Flashing also pushes the matching support files
> (`/cores/*`, `/bios/logo.bin`, homebrew overlays) onto the SD card automatically;
> `gw_update.tar` is provided for manual recovery only. ROMs are never bundled.
