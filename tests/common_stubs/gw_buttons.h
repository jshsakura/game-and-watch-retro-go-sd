/* Host-test stub for Core/Inc/gw_buttons.h. common.c includes this but does
 * not reference any of its macros/functions directly (input state comes in
 * as an odroid_gamepad_state_t*); kept as an empty placeholder so the
 * #include succeeds. */
#ifndef STUB_GW_BUTTONS_H
#define STUB_GW_BUTTONS_H
#endif
