/* Host-test stub for Core/Inc/gw_linker.h. common.c includes this but does
 * not reference any linker symbol directly (they're consumed by gw_malloc.c
 * instead, tested separately in tests/test_gw_malloc.c). Empty placeholder
 * so the #include succeeds. */
#ifndef STUB_GW_LINKER_H
#define STUB_GW_LINKER_H
#endif
