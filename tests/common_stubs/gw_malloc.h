/* Host-test stub for Core/Inc/gw_malloc.h. common.c includes this but does
 * not call any allocator directly; kept as an empty placeholder so the
 * #include succeeds. gw_malloc.c itself is tested separately (test_gw_malloc.c). */
#ifndef STUB_GW_MALLOC_H
#define STUB_GW_MALLOC_H
#endif
