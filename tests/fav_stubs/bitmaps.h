#ifndef STUB_FAV_BITMAPS_H
#define STUB_FAV_BITMAPS_H
/* rg_favorites.c only needs these two logo indices (passed straight through
 * to gui_add_tab, never dereferenced here). */
#define RG_LOGO_PAD_FAVORITES 0
#define RG_LOGO_HEADER_FAVORITES 1
#endif
