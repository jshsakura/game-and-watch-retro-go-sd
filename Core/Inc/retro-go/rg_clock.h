#pragma once

/* Full-screen Clock app launched from the TIME menu. A mode switcher over
 * the things you'd expect from a normal clock app: a live clock face, a
 * Pomodoro cycle, a countdown timer and a stopwatch. Alarm + SD theming
 * (backgrounds / fonts) layer on later. All faces are drawn in code — no
 * bundled art — so there is nothing copyrighted to ship. */
void rg_clock_show(void);
