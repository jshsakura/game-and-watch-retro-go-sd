# 32X SH-2 Interpreter ITCM Allocation Analysis (Deep Dive)

## 1. SH-2 인터프리터 구조 및 Threaded Dispatch의 한계
`external/picodrive/cpu/sh2/mame/sh2pico.c`와 `sh2.c`를 정밀 분석한 결과, **소스(Hot/Cold)를 분리하는 방식은 성능상 치명적인 퇴보를 가져옵니다.**
- **Threaded Dispatch 제약**: 해당 인터프리터는 `GNW_SH2_FASTLOOPS`를 통해 Computed Goto(`goto *gnw_dt[opcode >> 12];`) 방식의 디스패치를 사용합니다. C 언어 표준상 Computed Goto의 타겟 라벨(`gnw_op0` 등)은 반드시 **동일한 함수 내**에 존재해야 합니다.
- **분리 불가**: 만약 `INLINE` 매크로를 `noinline`으로 바꾸어 콜드 패스(희귀 옵코드)를 별도 함수로 빼낸다면, 디스패처는 Computed Goto 직후에 `call/ret`을 수행해야 합니다. 이는 명령어 1개마다 파이프라인 플러시와 함수 호출 오버헤드를 유발하여, 인터프리터를 ITCM에 올리려는 본래 목적(0-wait 성능 극대화)을 정면으로 부정하는 꼴이 됩니다.

## 2. ITCM 예산 정밀 계산 및 BSS 방출 후보 랭킹
현재 ITCM 여유 공간은 4832B이며, 인터프리터 전체(6572B)를 넣기 위해 **1740B**가 모자란 상황입니다. 소스 분리가 불가하므로, ITCM을 점유 중인 BSS 데이터 일부를 일반 빠른 램(`RAM_EMU`)이나 DTCM으로 방출하는 것이 유일하고 확실한 해법입니다.

### 방출 후보 랭킹 (성능 영향 / 리스크 기준)
1. **`PsndBuffer` (8664B)** - **최우선 권장 (1위)**
   - **근거**: `external/picodrive/pico/sound/sound.c:30`에 선언된 메인 사운드 믹싱 버퍼입니다. 사운드 믹싱은 CPU 사이클 단위가 아닌 프레임/버퍼 단위로 순차 접근하므로, 캐시 미스로 인한 성능 패널티가 거의 없습니다. 이를 `RAM_EMU`로 옮기면 8.6KB가 단번에 확보되며 가장 리스크가 적습니다.
2. **`.bss.ym2612` (3216B)** - **권장 (2위)**
   - **근거**: YM2612 FM 신디사이저 상태값입니다. 특히 32X Doom은 SH-2의 PWM 오디오를 주로 활용하므로 FM 칩의 엑세스 빈도가 낮습니다. 1740B 부족분을 깔끔하게 메울 수 있습니다.
3. **`bus map` (3840B) / `HighLnSpr` (7680B)** - **비권장 (3, 4위)**
   - **근거**: 메모리 맵과 VDP 라인 스프라이트는 CPU 메모리 엑세스 및 스캔라인 렌더링마다 쉴 새 없이 접근하는 초고빈도 핫패스입니다. 이를 방출하면 I-Cache 미스 이상의 D-Cache 미스가 발생하여 오히려 프레임이 떨어질 수 있습니다.

## 3. 조사관의 정직한 최종 판정
이번 SH-2 ITCM 이동 과제는 **(a) 간단히 해결할 수 있는 작업(BSS 이동)**입니다. 
- 복잡한 소스 분리(b)나 XIP 콜드패스(c)를 고민할 필요가 전혀 없습니다. Computed Goto의 무결성을 해치지 않기 위해 **6572B 인터프리터 전체를 한 덩어리로 ITCM에 넣어야 합니다.**
- 이를 위해 `PsndBuffer`나 `ym2612` 구조체에 `__attribute__((section(".ram_uc")))` (또는 해당 링커 매크로)를 달아 `RAM_EMU` 영역으로 방출하십시오. 즉시 1740B 룸이 확보되어 SH-2 인터프리터가 ITCM에 무혈입성할 수 있습니다.
- **부가 대안**: SH-2가 ITCM으로 들어가 캐시 경합(I-Cache thrashing)이 해소되면 프레임레이트 상승이 확실합니다. 이 작업 이후 다음 32X 최적화 레버로는 Doom의 극심한 인터럽트 오버헤드를 유발하는 **PWM timer yield 최적화** 혹은 **SDRAM 롬 패스트패스 고도화**를 타겟하는 것이 맞습니다.
